#!/usr/bin/env bash

BACKUP_DIR_PATH='/backups'
# the above  variable holds the path to the directory where backup files should be stored

BACKUP_FILE_PREFIX='backup'
# the above variable holds the prefix for naming backup files

